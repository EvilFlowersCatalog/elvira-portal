# A few leaf dependencies (react-spinners, react-gravatar) still declare
# conservative React <=18 peer ranges but run fine on React 19. Allow the
# install to proceed rather than pinning the whole app to React 18.
legacy-peer-deps=true
